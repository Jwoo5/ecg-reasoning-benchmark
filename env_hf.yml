name: ecg-reasoning-benchmark-hf
channels:
  - defaults
dependencies:
  - python=3.10
  - pip:
      - accelerate==1.12.0
      # - torch==2.5.1 # adjust for cuda version as needed
      # - torchvision==0.20.1 # adjust for cuda version as needed
      - ecg_plot==0.2.8
      - matplotlib==3.10.7
      - wfdb==4.3.0
      - tqdm==4.67.1
      - transformers==4.57.1
      - bitsandbytes==0.48.2