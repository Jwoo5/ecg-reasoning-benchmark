name: erb-vllm
channels:
  - defaults
dependencies:
  - python=3.12
  - pip:
      # - torch==2.9.0 # adjust for cuda version as needed
      # - torchvision==0.24.0 # adjust for cuda version as needed
      - vllm==0.11.2
      - qwen_vl_utils==0.0.14
      - ecg-plot==0.2.8
      - matplotlib==3.10.7
      - wfdb==4.3.0
      - tqdm==4.67.1
      - ftfy==6.3.1
      - scikit-learn==1.7.2
      - einops==0.8.1
      - einops_exts==0.0.4
