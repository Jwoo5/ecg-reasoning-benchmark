name: ecg-reasoning-benchmark-hf
channels:
  - defaults
dependencies:
  - python=3.12
  - pip:
      - accelerate==1.12.0
      # - torch==2.7.1 # adjust for cuda version as needed
      # - torchvision==0.22.1 # adjust for cuda version as needed
      # - flash-attn==2.7.3 # adjust for torch and cuda version as needed
      - ecg-plot==0.2.8
      - matplotlib==3.10.7
      - wfdb==4.3.0
      - tqdm==4.67.1
      - transformers==4.57.3
      - bitsandbytes==0.48.2
      - ftfy==6.3.1
      - scikit-learn==1.7.2